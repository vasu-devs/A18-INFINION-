# ─── Core Application ────────────────────────────────────────────────────────
pydantic>=2.0.0          # Data validation and settings management
python-dotenv>=1.0.0    # Loading .env variables
pandas>=2.0.0           # CSV processing in csv_io.py

# ─── LLM Clients ─────────────────────────────────────────────────────────────
openai>=1.30.0          # DeepSeek V3 and OpenAI compatibility
google-genai>=1.0.0     # Gemini Flash 2.0
groq>=0.4.0             # Groq Llama 3.3
aiohttp>=3.9.0          # Ollama REST API and general async HTTP
httpx>=0.27.0           # Model Context Protocol (MCP) transport

# ─── Model Context Protocol (MCP) ───────────────────────────────────────────
mcp>=1.1.2              # MCP Python SDK
fastmcp==2.14.5         # High-level MCP server framework

# ─── MCP Server & RAG Engine ────────────────────────────────────────────────
llama-index-core>=0.12.0
llama-index-embeddings-huggingface>=0.4.0
torch>=2.0.0            # Required for HuggingFace embeddings
transformers>=4.30.0    # Required for HuggingFace embeddings

# ─── CLI and UI (Optional but recommended) ───────────────────────────────────
typer>=0.9.0            # CLI argument handling
rich>=13.0.0            # Beautiful terminal formatting
